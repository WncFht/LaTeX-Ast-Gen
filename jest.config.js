/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
  preset: 'ts-jest',
  testEnvironment: 'node',
  // 可根据需要取消注释并调整以下选项：
  // roots: ['<rootDir>/test'], // 指定测试文件的根目录
  // testMatch: [
  //   '**/__tests__/**/*.+(ts|tsx|js)',
  //   '**/?(*.)+(spec|test).+(ts|tsx|js)',
  // ],
  // transform: {
  //   '^.+\\.(ts|tsx)?$': ['ts-jest', {ซื้อ // ts-jest 选项，例如 tsconfig 
  //     // tsconfig: 'tsconfig.test.json' // 如果测试有单独的 tsconfig
  //   }],
  // },
  // moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'node'],
}; 