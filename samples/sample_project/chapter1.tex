\section{第一章}

这是第一章的内容，通过 \verb|\input| 命令引入。

\subsection{图表}

我们可以插入图片和表格：

\begin{figure}[htbp]
    \centering
    % 实际使用时这里会是真实的图片
    \caption{示例图片}
    \label{fig:example}
\end{figure}

参考图 \ref{fig:example}。

\mycmd{参数1}{参数2}
\myoptcmd[自定义]{参数}

% 测试\tr和\myvec命令
$\tr(A) = \lambda_1 + \lambda_2$

$\myvec{v} = (x, y, z)$ 